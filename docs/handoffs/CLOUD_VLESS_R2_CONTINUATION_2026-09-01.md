# Cloud VLESS R2 continuation handoff — 2026-09-01

Repository: `k-kostin/omavless`

Status: **partial cloud session; R2g through R2h3 are merged, R2i and R2j remain unimplemented**.

Canonical verdict at this checkpoint:

`CLOUD VLESS R2 SESSION PARTIAL — R2i/R2j REQUIRE A FRESH MACHINE SESSION`

## Why this document exists

The cloud session successfully completed and recorded the deterministic VLESS transport and XHTTP parser slices through R2h3. Later attempts to automate R2i/R2j depended on temporary external patch/report artifacts. Those artifacts expired and GitHub Actions received HTTP 404 before applying any R2i code. Therefore no R2i or R2j implementation was accepted, no completion verdict is valid, and this file is the durable continuation record.

Do not infer R2i completion from old chat messages, expired sandbox files, failed CI workflows or issue comments generated by the failed automation.

## Exact accepted progression

Starting checkpoint before this long session:

- `main`: `0185e50b7c146ce899edfb689076befc93a7b436`

Accepted during the session:

1. PR `#72` — R2g VLESS transport-option parity
   - accepted head: `8f65628f57b8a7caaa801beb6b44a3a7894bbed6`
   - merge: `256669d7155766c9d1a0e05fe41a186cbd639458`
   - differential: 34 credential-free cases
2. PR `#73` — R2g ledger
   - accepted head: `9b22af444a786e281637f46a732a6150d4e24cc2`
   - merge: `22732003bf16bcc21ceaf0cca433703fa8dfdd4d`
3. PR `#74` — R2h1 XHTTP `extra` decoder/shape parity
   - accepted head: `bf8aa50e3b3c1fa67cc9baf0924fe38d2a5da469`
   - merge: `a97871703c27c403f8be715889daa82ee755821e`
   - differential: 62 credential-free cases
4. PR `#75` — R2h1 ledger
   - accepted head: `c63ea4ab7bb29728013539f51efd1daeb6150e6d`
   - merge: `eb008a672bf14e9d80a0dec32595f8872c1f737e`
5. PR `#78` — R2h2 normalized top-level XHTTP-option parity
   - accepted head: `50ae48f73448c720c41d89defe7d843ac0ed35ba`
   - merge: `fb08101e7d15b5484a1c4d729cf715e171b87161`
   - differential: 115 credential-free cases
6. PR `#79` — R2h2 ledger
   - accepted head: `00338dac309d06dd0607f25e295f6cab4345c61b`
   - merge: `07d118a3f8c613df59dcea2c2b5549bacee4d8e5`
7. PR `#80` — R2h3 XHTTP `downloadSettings` parity
   - accepted head: `58781daebb728a12d2639a5621d698982cfd03db`
   - merge: `f4c9d9eccfd0b76439c66265b700c19847b7f445`
   - differential: 188 credential-free cases
8. PR `#81` — R2h3 ledger
   - accepted head: `c81960ae99af8625bc2d6cd6dace1230c4949b12`
   - merge/current checkpoint `main`: `a60d840d6901cebe7a90d79ce38da6bbe70e21aa`

## Current truthful state

- R2i implementation PR: **none**.
- R2i ledger PR: **none**.
- R2j cleanup/audit PR: **none**.
- Pure VLESS parity complete: **no**.
- R2 overall complete: **no**.
- Production cutover performed: **no**.
- Python remains the installed production implementation and migration oracle.
- The current plugin, private store, QML, service, controller, Mihomo lifecycle, TUN and routing ownership were not switched to Rust.
- No private credentials or provider fixtures were intentionally committed by this work.

## Failed automation — do not resume it

The following branches contain cloud scaffolding/workflows, not an accepted R2i implementation:

- `ci/cloud-r2i-workspace`
- `ci/apply-r2i-cloud-patch`
- `ci/complete-r2i-r2j-idempotent`
- `ci/r2i-compile-probe-v1`

The latest completion workflow failed at its initial `r2i-artifacts` download because temporary external artifacts returned HTTP 404. Issue `#82` was created by that failure. Its blank-stage comments are an automation quoting defect, not useful acceptance evidence.

A machine agent must implement R2i from current source and tests. It must not depend on those expired patch artifacts or treat any failed workflow as reusable acceptance.

## Immutable V0 / PR #30 boundary

PR `#30` must remain Draft at exact head:

`a643db595ad5369b5fea200ebc601b4f0f70f18f`

Its accepted representative VLESS XHTTP `stream-one` live evidence remains valid for that exact head. Do not rebase, modify, merge or close PR #30 merely because `main` advances. Missing VLESS Encryption/REALITY-PQ, Trojan, Hysteria2 and TUIC fixtures do not block deterministic R2i/R2j work.

## Required next work — R2i

Start a fresh branch from the latest remote `main`, expected at this checkpoint to include merge `a60d840d6901cebe7a90d79ce38da6bbe70e21aa`:

`codex/rust-vless-canonical-rendering`

Read completely before editing:

- `AGENTS.md`
- `DEVELOPMENT_ROADMAP.md`
- `docs/roadmap/RUST_MIGRATION.md`
- `docs/roadmap/DEVELOPMENT_WORKFLOW.md`
- `docs/roadmap/ACCEPTANCE_ENVIRONMENTS.md`
- `backend.py`: `parse_vless`, `preview_vless`, `_vless_subscription_key`, `proxy_yaml` and every helper they call
- all accepted Rust VLESS/XHTTP modules and differential corpora

Implement one reviewable pure-adapter slice containing:

1. A canonical private VLESS model composed from the accepted authority, query, REALITY/PQ, Encryption, transport-option and XHTTP modules without duplicating validators.
2. Credential-safe preview parity.
3. Exact subscription-identity normalization/parity, using private canonical fingerprints rather than publishing normalized credentials.
4. Semantic Mihomo proxy rendering parity for TCP/raw, WS, HTTP/H2, gRPC and XHTTP, including TLS/REALITY, Vision, packet encoding, Encryption and accepted XHTTP/download settings.

Hard boundaries:

- no production cutover;
- no private-store or service schema migration;
- no daemon, CLI, TUI or QML integration;
- no controller, TUN, routing or Mihomo lifecycle change;
- no other protocol implementation;
- no raw URI, UUID, endpoint, SNI, keys, paths, hosts, services, headers or XHTTP tokens in public reports, errors or `Debug`;
- do not touch PR #30.

Build a bounded synthetic Python/Rust differential that covers pairwise transport/security combinations, Vision, packet encoding, REALITY/PQ parsing facts, Encryption, transport fields, XHTTP modes/options/download settings, identity invariants, preview redaction and rendered Mihomo semantics. Avoid an unbounded Cartesian product; document the coverage rationale.

Required portable gates:

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo test --workspace --locked
./tests/run-rust.sh
./tests/run.sh
python3 -m py_compile backend.py tools/*vless*parity.py tools/*xhttp*parity.py
bash -n backend.sh install.sh uninstall.sh tests/run.sh tests/test-qml.sh tests/run-rust.sh
jq -e . manifest.json
cargo metadata --locked --no-deps

git diff --check
```

Run `omarchy plugin validate "$PWD"` when available. For this non-production Rust adapter, absence of the Omarchy tool in CI may be recorded as `NOT RUN — TOOL UNAVAILABLE`; no live VPN smoke is required because production does not import the candidate.

Open a non-Draft R2i implementation PR. Record exact base/head, changed-file scope, differential count, privacy review, every gate and the fact that Python remains production owner/oracle. Merge only with green exact-head CI and unchanged scope.

## R2i ledger

After the implementation merge, create a separate docs-only branch/PR. Update the canonical continuity files that require the new checkpoint, normally:

- `AGENTS.md`
- `DEVELOPMENT_ROADMAP.md`
- `docs/roadmap/RUST_MIGRATION.md`

Record exact implementation PR/head/merge, differential count, private/public ownership, remaining production-cutover work and that R2 overall remains incomplete for non-VLESS adapters/downstream stages. Do not rewrite immutable release/V0 history.

## R2j consolidation audit

After the R2i ledger merges:

- audit all pure VLESS adapters and error catalogs for real duplication;
- verify all stdin/stdout/stderr bounds;
- verify private-model `Debug` and error redaction;
- run `cargo metadata --locked` and inspect dependency direction;
- run the full strict Clippy, Rust, Python/QML and privacy suites;
- document remaining production-cutover, packaging and rollback work.

If real cleanup is needed, use one small tested PR. If no code change is needed, do not create a ceremonial implementation commit; instead place the final R2j receipt in the merged R2i ledger discussion or a dedicated canonical audit document.

The final truthful verdict after successful R2i + ledger + R2j is:

`CLOUD VLESS R2 SESSION COMPLETE — PURE VLESS PARITY MERGED`

This verdict means pure VLESS candidate parity is merged. It does **not** mean Python production cutover or overall R2 completion for Trojan/Hysteria2/TUIC.

## Repository cleanup after handoff is merged

Delete only the obsolete R2i cloud-scaffolding branches listed above once this document is reachable from `main`. Keep PR #30 and its head branch intact. Update issue #82 to point here; close it only after the machine agent has either opened a real R2i PR or explicitly superseded the failed automation with a new tracked implementation branch.
