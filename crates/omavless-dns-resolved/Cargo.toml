[package]
name = "omavless-dns-resolved"
description = "Uninstalled fixed-purpose resolved wire conformance boundary"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
omavless-dns-tun = { path = "../omavless-dns-tun" }
async-io = "=2.6.0"
futures-lite = "=2.6.1"
serde = { version = "1.0", features = ["derive"] }
zbus = "=5.19.0"

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
