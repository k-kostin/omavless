[package]
name = "omavless-dns-transaction"
description = "Effect-free DNS transaction conformance model; not a host authorization provider"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints]
workspace = true
