[package]
name = "omavless-profile"
description = "Canonical profile protocol model and pure adapters for OmaVLESS"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
base64 = "0.22"
idna = "1.1"
serde_json = "1.0"

[dev-dependencies]
omavless-parity = { path = "../omavless-parity" }
serde = { version = "1.0", features = ["derive"] }

[lints]
workspace = true
