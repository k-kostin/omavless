[package]
name = "omavless-dns-channel"
description = "Uninstalled fixed-purpose credential-checked Unix DNS lease channel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
rustix = { version = "=1.1.5", features = ["event", "fs", "net", "process"] }

[dev-dependencies]
omavless-dns-tun = { path = "../omavless-dns-tun" }
tempfile = "=3.27.0"
nix = { version = "=0.30.1", features = ["socket"] }
serde_json = "1.0"

[lints]
workspace = true
