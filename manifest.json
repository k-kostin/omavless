{
  "schemaVersion": 1,
  "id": "kdk.omavless",
  "name": "OmaVLESS",
  "version": "0.6.2",
  "author": "kdk",
  "license": "MIT",
  "description": "Import, subscribe to and switch VLESS profiles from the Omarchy bar using a dedicated Mihomo TUN service, with grouped providers, endpoint latency, visible routing policy and live traffic.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "OmaVLESS",
    "description": "Toggle, switch, import and subscribe to VLESS profiles backed by Mihomo. Shows the effective routing mode and ships a RoscomVPN-based split-routing policy.",
    "category": "Network",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "refreshIntervalSec": 10,
      "pingHost": "1.1.1.1",
      "showBarThroughput": false,
      "showExitIp": true
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh interval (seconds)",
        "min": 2,
        "max": 3600,
        "step": 1,
        "defaultValue": 10
      },
      {
        "key": "pingHost",
        "type": "string",
        "label": "Ping host (empty disables the latency probe)",
        "defaultValue": "1.1.1.1"
      },
      {
        "key": "showBarThroughput",
        "type": "boolean",
        "label": "Show live throughput in the bar",
        "defaultValue": false
      },
      {
        "key": "showExitIp",
        "type": "boolean",
        "label": "Show the observed exit IP",
        "defaultValue": true
      }
    ]
  }
}
